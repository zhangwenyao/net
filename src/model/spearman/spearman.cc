#include "spearman.h"
#if defined(NET_SPEARMAN) && defined(STAT_SPEARMAN)

#include "../../common/common.h"
using namespace std;
using namespace common;
using namespace network;

// ******************************************************
int network::spearman::network_guass(const NodeType M, const NodeType M0,
    const NodeType nodeSize, VVNodeType& p2p, NodeType& kMin, NodeType& kMax)
{
  return 0;
}

// ******************************************************

#endif
